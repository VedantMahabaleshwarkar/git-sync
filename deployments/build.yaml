apiVersion: v1
kind: List
items:

- kind: "BuildConfig"
  apiVersion: "v1"
  metadata:
    name: "git-sync" 
  spec:
    
    runPolicy: "Serial" 
    
    triggers:
      - type: ConfigChange
      - github:
          secret: git-sync
        type: GitHub
    
    source: 
      git:
        uri: "https://github.com/stufff-ml/git-sync"
        ref: "master"
      #contextDir: "airflow"
    
    strategy:
      type: Docker
      dockerStrategy:
        dockerfilePath: Dockerfile
        forcePull: true
        
    output:
      to:
        kind: ImageStreamTag
        name: 'git-sync:latest'

- kind: ImageStream
  apiVersion: "v1"
  metadata:
    annotations:
      description: A command for periodically syncing a git repository
      tags: python, git
    name: 'git-sync'